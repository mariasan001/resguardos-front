BACKEND_API_URL=http://localhost:9999

# Secreto para firmar la cookie de sesión del frontend (no es el JWT del backend).
AUTH_SECRET=change-this-secret-before-production

# Credenciales de desarrollo del backend (Swagger / usuario_sistema):
# admin / Admin123!
# encargado / Encargado123!
